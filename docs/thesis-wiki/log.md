# Change Log

Chronological record of wiki updates and research discoveries.

---

- **2026-04-25** — Wiki created. Initial structure: 5 state-of-art, 5 methodology, 3 experiments, 1 decisions page.
- **2026-04-25** — Ingested `investigation.md` partial spoof section into wiki. Added 7 splice technique pages (13c–13j) to presentation.
- **2026-04-25** — Key discovery: all 7 crossfade techniques sound identical in listening tests. The real problem is word boundary alignment (TTS generates fluid speech, no energy dip at "Dame"→"únicamente") and duration mismatch (480ms clone in 640ms slot shifts rhythm 160ms).
- **2026-04-25** — Implemented valley-score word selection: `score = min_rms / avg_rms` in ±100ms window, threshold 0.65. Words with no energy valley (score > 0.65) are ineligible. "el" (idx 2) scores 0.165 — deep valley. "únicamente" scores 0.700 — fluid boundary, rejected.
- **2026-04-25** — Implemented duration-preserving splice: time-stretch cloned word to fit exact bonafide slot, overwrite `result[b_start:b_end]` in-place. Total audio length never changes.
- **2026-04-25** — Implemented clone similarity gate: ECAPA-TDNN cosine SIM ≥ 0.60 between bonafide and clone. Runs between Step 2 and Step 3. Known rates: FishGram avg 0.602 (~50% pass at 0.60), Qwen avg 0.720 (most pass), OpenVoice avg 0.394 (most fail).
- **2026-04-25** — Production status updated: FishGram DONE (95.2%), Qwen DONE (87.9%), OpenVoice DONE (83.4%), Chatterbox 41% running (ETA May 13), OuteTTS 66% running (ETA May 1).
- **2026-04-25** — Added slides 06b (Chatterbox progress), 06c (OuteTTS progress), 13a–13l (splice quality problem + solution). Total: 34 slides.
- **2026-04-25** — v2 pipeline pending validation on ml-server03: 5 speakers (arf_00295, arf_00610, arf_01523, arm_00412, arm_00780), ≥10 audios each, using fishgram_env + GPU 1.
- **2026-04-30** — Added OmniVoice (k2-fsa) as 6th attack pipeline. New folder `app/pipeline/omnivoice_attack/` with 5 canonical steps, isolated venv `envs/omnivoice_env/`, torch 2.8.0+cu126 to match ml-server03 driver. Reference text pre-computed with Parakeet (not Whisper). Native 24 kHz output resampled to 16 kHz on FLAC write. Audio ID range 15M (originally 12M, fixed 2026-05-01 after discovering collision with partial_spoof main W1). Spanish training data: 27,559 hours. Pending standalone validation run before joining boundary jitter suite.
- **2026-05-01** — Implemented boundary jitter feature for partial_spoof (Step 5b `BoundaryJitterApplier`). Three structural manipulations: truncate (10-40 ms, sub-syllable cuts), overlap (30-80 ms, matches LlamaPartialSpoof crossfade range, Hanning fade), bleed (20-60 ms, covers Spanish VOT + consonant-vowel transition). Per-boundary coin flip with `JITTER_PROBABILITY=0.5`; spoof boundaries also receive the coin flip on top of the existing splice. Targets the "find the noisy boundary" detector shortcut documented in Negroni et al. (2024) and Muller (2024). Boundaries processed right-to-left to avoid invalidating earlier sample positions. Total length drift bounded; recorded per utterance.
- **2026-05-01** — Added per-speaker bonafide partition (`BONAFIDE_FILE_PARTITION` = "main" | "jitter") so the jitter dataset uses sentences disjoint from main partial_spoof. Files shuffled per-speaker with deterministic seed (`BONAFIDE_PARTITION_SEED + sha256(speaker_id)`) and split 50/50. No speakers discarded (1-file speakers contribute to whichever partition the shuffle assigned). Jitter outputs use system_id `<ATTACK>_PSW{N}J` and audio ID range 16M-18M (W1J=16M, W2J=17M, W3J=18M).
- **2026-05-01** — Decided: Qwen is the boundary jitter pilot, NOT OmniVoice. Qwen is already validated (ECAPA SIM 0.720), so any change in detector EER on Qwen+jitter is attributable to jitter alone, not TTS quality. OmniVoice joins the jitter suite only after standalone validation passes.
- **2026-05-01** — Wiki updates: decision-log gained 5 entries (OmniVoice integration, boundary jitter design, partition strategy, Qwen pilot decision, audio ID collision fix). methodology/partial-spoof-approach.md gained "Boundary Jitter Variant" section. methodology/attack-systems.md gained OmniVoice entry. state-of-art/tts-systems.md gained OmniVoice section. state-of-art/splicing-techniques.md gained "Boundary Jitter" section with literature/phonetics grounding for each magnitude range.
- **2026-05-01** — Strengthened methodology/partial-spoof-approach.md "Boundary Jitter Variant" section with: (a) explicit two-folders-per-attack table (main + jitter, with paths/system_ids/audio_id ranges), (b) full random selection algorithm in pseudocode (coin flip -> uniform op pick -> uniform magnitude -> uniform side/direction), (c) max-entropy justification for `JITTER_PROBABILITY=0.5` with distribution table comparing p in {0.3, 0.5, 0.7, 1.0}, (d) least-informative-prior justification for uniform pick among ops, (e) reproducibility note (sha256-seeded RNG, not Python `hash()`), (f) independence between boundaries.
- **2026-05-01** — Added "Operational Runbook" + "Pending Work Checklist" to experiments/production-runs.md so the wiki is now a self-contained handover document. Includes exact ml-server03 commands for: (1) OmniVoice venv setup + validation run, (2) Qwen partial_spoof jitter pilot, (3) deferred detector EER comparison. Explicit success criteria per job. Pre-requisites table (data, venvs, GPU). Index page updated to flag the runbook as the entry point for continuing on a new machine.
- **2026-05-06** — OmniVoice venv (envs/omnivoice_env/) rebuilt on ml-server03 after dependency conflict between NeMo's transformers pin and modern transformers. Working order: torch (cu126) -> nemo_toolkit[asr] + setuptools<81 -> --no-deps omnivoice -> force-reinstall transformers>=5.3.0 --no-deps -> CRITICAL pin huggingface_hub==1.5.0 --no-deps to fix `is_offline_mode` ImportError cascading through lightning/torchmetrics. fishgram_requirements.txt confirmed as the canonical pin reference for the transformers/hf_hub/lightning/torchmetrics quartet (5.3.0/1.5.0/2.6.1/1.8.2). Production-runs.md Job 1 recipe updated.
- **2026-05-06** — OmniVoice standalone validation PASSED on GPU 1. 6/6 samples (3 speakers, 2 each), avg WER 3.94%, CER 1.81%, NISQA 4.53, ECAPA SIM 0.680. NISQA highest of suite; ECAPA SIM is below 0.70 informational floor (OmniVoice is the **weakest cloner** of the 6-attack suite by speaker similarity, contrast: Qwen 0.720, FishGram 0.602, OpenVoice 0.394). Cleared for production mode. Cleared for boundary jitter pilot inclusion. validation-results.md and production-runs.md updated with metrics; attack suite memory updated.
- **2026-05-06** — Surfaced and fixed an OmniVoice-specific failure mode: reference voice bleed at audio start (200-600 ms fragment of the reference speaker before the prompt content). Parakeet TDT does not transcribe the fragment (sub-syllabic), so WER stays at 0.0 and the existing word-alignment-based prefix detector misses it. Master Tomas's ear flagged 2/6 samples (TEXT_00001, TEXT_00002 from `arf_00295`) where validated_samples.json reported WER=0 yet audio was clearly contaminated. Diagnostic script (`app/scripts/diagnose_omnivoice_prefix.py`) confirmed: artifact samples have pre-speech RMS in [-25, -22] dB while clean samples sit at -120 dB silence floor — a 95 dB discriminator margin.
- **2026-05-06** — Implemented two-layer prefix detection + retry loop. (1) New `detect_nonverbal_prefix_artifact` in `app/utils/prefix_trimmer.py` flags samples where pre-speech RMS exceeds `NONVERBAL_PREFIX_RMS_FLOOR_DB = -55.0`. (2) Step 4 of OmniVoice rejects flagged samples instead of trimming (avoids cutting natural Spanish vowel onsets). (3) New `_execute_omnivoice` in `app/runner/production_runner.py` mirrors the Qwen/FishGram pattern and wires `_retry_rejected` so rejected samples regenerate up to 5 rounds. Validation re-run: 100% recall (12/12 bleed instances detected across initial + 5 retries on 2 samples). However, both `arf_00295` samples bled on every one of 6 generation attempts — reveals a deterministic per-speaker failure mode that retries cannot fix. Reference-selection follow-up needed before production.
- **2026-05-06** — Added presentation slide `01e_omnivoice_overview.html` covering the OmniVoice model facts (646 languages, 27,559 h Spanish, RTF ~0.025) and the prefix-bleed handling pipeline (Layer 1 word-align trim, Layer 2 pre-RMS reject, retry up to 5x). Updated wiki: methodology/attack-systems.md (new "OmniVoice prefix-bleed handling" subsection), methodology/quality-metrics.md (new "Non-Verbal Prefix RMS" metric and threshold-summary row), decisions/decision-log.md (new 2026-05-06 entry).
- **2026-05-06** — **Root cause of OmniVoice prefix bleed identified and fixed.** Master Tomas listened to `arf_00295_ref.wav` and reported the final phrase was cut. Investigation revealed the bug in `app/pipeline/omnivoice_attack/utils/audio_concatenation.py`: the routine sliced the last bonafide file at the exact sample boundary needed to hit a 10 s target duration, landing the cut mid-word. OmniVoice's diffusion conditioning attempted to "complete" the cut-off pattern at the start of generation -- which is the bleed we observed. The same bug exists in all 7 other attack pipelines but only OmniVoice is affected because diffusion-LM is uniquely sensitive to abrupt-end conditioning. Fix: stop at last fitting file (never slice mid-file), snap to nearest silence in edge case (single file > 10 s), always append 200 ms trailing silence. Post-fix validation: 6/6 PASSED first attempt, zero non-verbal-prefix rejections, zero retries needed; arf_00295 now passes cleanly. All metrics improved (WER 0.0394 -> 0.0185, CER 0.0181 -> 0.0083, NISQA 4.53 -> 4.59, ECAPA SIM 0.680 -> 0.696). Earlier "deterministic per-speaker failure mode" finding is retracted -- arf_00295's behavior was a consequence of the buggy reference, not a property of the speaker. The non-verbal-prefix detector + retry loop are kept as defense in depth. Wiki: attack-systems.md "OmniVoice prefix-bleed handling" rewritten with corrected story, new "Future improvement: retrofit fix to other pipelines" subsection added; decision-log.md gained the corrected root-cause entry; validation-results.md gained a three-stage timeline (initial / detector / post-fix); production-runs.md updated with post-fix metrics.
- **2026-05-10** — Presentation audit and route fixes. Fixed broken paths in three jitter slides (`13mb`/`13mc`/`13md_jitter_audio_*.html`) that pointed to a non-existent `data/spanish_dataset/bonafide_artition_Dataset_by_Speaker/...` path; now point to `data/bonafide_dataset_by_speaker/arf_00295/train/...`. Fixed two `data/qwen_partial_spoof/spliced/...` references in `13n_demo_partial_spoof.html` to use the canonical `data/attacks/qwen_partial_spoof/...` prefix. Fixed `13n` bonafide reference (was pointing to non-existent `bonafide_dataset_by_speaker_v2` locally). Resolved merge conflict in `14_next_steps.html` (5-step structure preserved, step 05 wording: "Continue writing the paper"). Created `09a_demo_omnivoice.html` (positive demo slide with WER 0% / NISQA 4.99 / SIM 0.714 metrics from `OMNIVOICE_arf_00295_OMNIVOICE_TEXT_00002.wav`).
- **2026-05-10** — Added ad-hoc cloning script `scripts/clone_my_voice.py` for OmniVoice voice-cloning sanity checks outside the production pipeline. Accepts MP3/M4A/WAV reference (auto-converts non-WAV via librosa to 16 kHz mono), optional `--ref-text` (Parakeet TDT auto-transcribes when omitted via the same singleton used in pipeline Step 4 validation), generates one output WAV at 24 kHz native OmniVoice sample rate. Settings reused from `app.pipeline.omnivoice_attack.settings` (DRY). Requires `PYTHONPATH=.` prefix when run from repo root. Used to validate end-to-end voice cloning with a 15 s personal reference recording; RTF 0.454 measured on GPU 1.
- **2026-05-10** — IEEE paper first draft written in `IEEE/conference_101719.tex`. Replaced the entire IEEE template with a real submission-ready draft titled "HABLA-Spoof: A Multi-System Partial Spoof Corpus for Latin American Spanish Anti-Spoofing with Boundary Jitter Adversarial Augmentation." Sections: Abstract, Introduction (3 structural problems: generalization crisis / reproducibility gap / Latin American absence + 5 contributions), Related Work (ASVspoof, 4 canonical partial spoof pipelines, Negroni / Huang / Müller analyses), Methodology (HABLA v2 bonafide, attack pipeline architecture, 6 TTS systems with production status, partial spoof 7-step pipeline with valley score and duration-preserving splice, 7 crossfade techniques, boundary jitter with $p{=}0.5$ max-entropy justification and 3 operations), Production Results table, Discussion (4 detector shortcuts eliminated, threat model, ethics), Conclusion + 4 future-work directions, 22-entry bibliography. Placeholders: advisor name, funding source. Pending: Fig. 1 pipeline diagram (referenced as `fig_pipeline.pdf`).
- **2026-05-20** — Manifest-driven partial spoof dispatch. Added pre-flight script (`app/scripts/generate_partial_spoof_manifest.py`) that transcribes the full HABLA v2 corpus with Parakeet once and writes `data/manifests/partial_spoof_plan.csv` (~35,927 rows) plus `partial_spoof_plan_summary.json`. Attack weights 40% OmniVoice / 20% Qwen / 10% each FishGram/OpenVoice/Chatterbox/OuteTTS assigned per-speaker via deterministic multinomial draw. Tier eligibility (W1/W2/W3) pre-computed from word count. Wired into `BonafideTranscriber` (manifest mode short-circuits Parakeet), `ClonedSpeechGenerator` and `AudioSplicer` (per-audio CheckpointManager), `OutputFormatter` (emits `samples.csv` + `spoofed_words.csv` per pipeline). Added top-level `PartialSpoofOrchestrator` (`app/runner/partial_spoof_orchestrator.py`) with single / aggregate / runbook / status modes -- aggregate concatenates the 12 per-pipeline CSVs into `corpus_samples.csv` + `corpus_spoofed_words.csv` + `corpus_summary.json`.
- **2026-05-20** — Step 6 metrics-only mode (keep-bad-stuff principle). `ENABLE_STEP_6_REJECTION=False` by default; WER/CER no longer drop samples, only label them via `quality_flag` ('high' / 'medium' / 'low'). Clone gate (ECAPA SIM >= 0.60) kept at the Step 2 -> 3 boundary as upstream non-attack filter. Generation errors retried up to `MAX_GENERATION_RETRIES=3`; quality failures never retried.
- **2026-05-20** — Partition rename: `main` -> `not_jittered`, `jitter` -> `jittered`. Updated `settings.BONAFIDE_FILE_PARTITION` default + validator, `step_01_transcribe_bonafide._apply_partition`, manifest schemas, pipeline_config field descriptions, AUDIO_ID range descriptions. Existing partial_spoof_output uses old labels and is being wiped per production refresh. Output layout changes to `data/partial_spoof_output/<attack>/<partition>/` under manifest mode.
- **2026-05-20** — Added schemas `ManifestEntry`, `ManifestSummary`, `GenerationFailure`, `CheckpointState` (one-class-per-file under `partial_spoof/schemas/`). Added utils `TierEligibilityComputer`, `ManifestGenerator`, `ManifestLoader`, `CheckpointManager`. Added settings keys `ATTACK_WEIGHTS`, `ATTACK_ASSIGNMENT_SEED`, `MANIFEST_PATH`, `MANIFEST_SUMMARY_PATH`, `MANIFEST_SLICE_ATTACK`, `MANIFEST_SLICE_PARTITION`, `ENABLE_CHECKPOINT_RESUME`, `MAX_GENERATION_RETRIES`, `ENABLE_STEP_6_REJECTION`. Pipeline config gained `use_manifest`, `manifest_path_override`, `manifest_slice_attack_override`, `manifest_slice_partition_override`.
- **2026-05-20** — Added `ParallelLauncher` (`app/runner/parallel_launcher.py`) with reap-and-relaunch policy for same-GPU concurrency. Each child runs in its own venv via `bash -c "source ... && python -m app.runner.partial_spoof_orchestrator --mode single ..."`. Default 4 concurrent on a 46 GB A40 (~8 GB/pipeline). Wired `--mode parallel` into the orchestrator CLI with `--max-concurrent`, `--order` (weighted / alphabetic / slow_first), `--no-skip-complete`. SIGINT propagates SIGTERM to children for crash-safe shutdown. Per-job logs at `logs/parallel_<attack>_<partition>.log`.

- **2026-05-24** — **Strategy -> Cloner refactor** (one source of truth per attack). Eliminated 10 files under `partial_spoof/strategies/` (duplicate per-attack cloning code) + `strategy_factory.py`. Added 6 new `<attack>_attack/utils/cloner.py` modules subclassing new `app/utils/base_cloner.BaseCloner` ABC, plus `partial_spoof/utils/cloner_dispatcher.py` for lazy attack import. Consolidated 8 copies of `audio_concatenation.py` into `app/utils/audio_concatenation.py` with thin re-export shims. Surfaced 5 silent bugs in the deleted strategies (OpenVoice non-existent `TONE_CONVERTER_CONFIG` setting, OuteTTS non-existent `OUTETTS_TEMPERATURE`, Chatterbox half-correct SDPA patch, OmniVoice missing `language=` arg, Qwen hardcoded `x_vector_only_mode`). Both standalone Step 3 and partial_spoof Step 2 now call the same `cloner.clone_single()`.

- **2026-05-24** — **Splice engine architectural rewrite** (8 bugs in one audit session). (1) Step 3 alignment_metadata and Step 5 splice_metadata now accumulate across regen rounds (previous overwrite cost samples in W1/W3 retry rounds). (2) `splice_seed = (settings.RANDOM_SEED + hash(key)) & ((1<<63)-1)` to mask sign bit (negative `hash()` was crashing `np.random.default_rng` with "expected non-negative integer", silently rejecting many W1/W3 splices). (3) New `app/pipeline/partial_spoof/utils/energy_refiner.py` corrects Parakeet drift on phrase-merged words like "la casa"; conservative gate (only refine if Parakeet centre falls in silence) prevents fragmentation of words with internal stop closures like "algunas". (4) Valley snap made asymmetric outward-only (`direction="earlier"` for start, `"later"` for end) so the slot can only expand, never shrink inward and leave bonafide ghost. (5) **Removed time-stretch entirely.** Linear-interp resampling shifted pitch ("cartoony/grueso" artefact); replaced by natural-duration insert + `cumulative_offset_samples` to map original-bonafide positions onto current-result for W2/W3. (6) Crossfade redesigned to live at the SEAMS (not inside a fixed slot); cloned source extended by `cf` samples to land the fade in TTS silence padding around the word (prevents "Luga-" truncation). (7) Silent-run gate (`_silent_run_backward/_forward`) bounds the cloned-side extension so neighbouring cloned words do not leak into the seams. (8) `bonafide_start_s`/`bonafide_end_s` in `splice_metadata.json` now refer to positions in the SPLICED audio (where the spoof region lives) rather than original-bonafide coordinates, since the corpus only consumes per-word labels relative to the partial spoof WAV. Validation 2026-05-24 (12 cells, manifest mode): yield 151 -> 295 samples (1.95x), runtime 1h 8min, zero alignment-missing/seed-crash warnings, audit auditiva approved.

- **2026-06-08** — **Data-augmentation pipeline audited; new methodology page + Option B decided.** Read the full augmentation codebase (`app/augmenter/` base+RIR+Codec+RawBoost, `augmentation_config.py`, `schema.py`, `utils.py`, `augmentation_calculator.py`, `augmentation_pipeline.py`). Findings: (1) TWO label-leak shortcuts -- clean fraction coupled to class (balanced mode uses different per-class factors -> clean% = 1/factor diverges; e.g. bonafide 8x=12.5% clean vs spoof 2x=50% clean) and per-augmentation-type loudness normalization (clean=natural, RawBoost/Codec=-20 dB RMS, RIR=peak only). (2) "RawBoost" is not RawBoost (no LnL multiband+Hammerstein, no ISD, no SSI; random short FIR + weak tanh alpha[0.1,0.5] + low Gaussian noise + a gain op that RMS-normalization cancels exactly + clipping@0.9 p=0.2). (3) "Codec" is not a codec (codec_types list unused; resample 8k + bandpass + zeroed packets + linear quant; no companding/AMR/Opus/PLC). (4) Correctness bugs: RIR `fftconvolve mode='same'` smears/shifts reverb (should be 'full' aligned to direct peak), packet-loss metadata double-draw, dead `t60_range`. The student's symptoms (weak NL, dead clipping, stray white noise, useless AGC) are correct but caused by a broken impl, not lack of realism -- his "alternatives" (Hammerstein/multiband, impulsive signal-dependent, white-through-FIR) ARE real RawBoost. Decisions: **Option B** (uniform per-class augmentation factor -> no leak; balance in the trainer via weighted loss/sampler), uniform loudness across all paths, proven libraries (official RawBoost + torchaudio/ffmpeg codecs), all-threats codec coverage, front-end deferred (corpus stays waveform-level, no polarity/SpecAugment baked in). Created `methodology/data-augmentation.md` (full synthesis), index.md row added, decision-log entry 2026-06-08. Tier A (Option B + uniform loudness + quick correctness) mandatory before any trustworthy training run; do not train on existing `data/augmented/`.
- **2026-06-06** — **Data-augmentation feedback review (student investigation) -- in progress.** Ingested two documents: `spoofing_DA.pdf` (12-page investigation "Pertinencia de los modelos de aumentacion de datos actuales", with dataset spectrograms and per-augmentation critiques) and `feedback_da_antispoofing.pdf` (5-page point-by-point review keyed to Layer 3A RIR/CODEC, Layer 3C). Core fault line identified: the student reasons from a **realism prior** (augmentation must mimic real channels/devices), the project reasons from a **robustness prior** (augmentation regularizes and expands distribution support; physical plausibility optional). Knockout argument captured for the student: his own citations defeat the realism critique -- RawBoost (his ref [3]/[4]) and SpecAugment (ref [2]) are deliberately non-physical, and he calls the LF filter's arbitrary spectral dips a "Problema" while proposing SpecAugment masking (the same mechanism) as the alternative. Verdict table drafted (ACCEPT/REJECT/DECIDE/VERIFY per point). Only one row decided so far: **RIR sourcing** (see decision-log 2026-06-06). Open gating questions still unanswered: (1) detector front-end (raw-waveform vs spectrogram -- gates polarity inversion and SpecAugment applicability), (2) deployment threat model (LatAm phone/call-center fraud over narrowband telephony? -- gates MUSAN-babble and realistic-VoIP-codec value). Next: read the actual augmentation pipeline code to settle the LF-arbitrary-dips (feature vs over-aggression bug), the "is it full RawBoost or a simplified version" question, and the white-noise-mislabel claim. Hard constraint noted that even the aggression prior must respect: **label preservation** -- augmentation must not erase the spoofing artifact (would inject label noise).
- **2026-06-06** — **RIR augmentation source confirmed = openSLR RIRS_NOISES (SLR28), Ko et al. ICASSP 2017.** Directory `data/noise_dataset/RIR/` has the signature layout `pointsource_noises / real_rirs_isotropic_noises / simulated_rirs / README`. The `small/medium/large` stratification comes from `simulated_rirs/`; only the room-size class is exposed, T60/Ds were sampled internally and are not per-RIR metadata. Decision: keep the canonical class names, cite Ko et al. 2017 / SLR28 (the student's own ref [3]), state the metadata limitation in methodology, and REJECT the student's rename-to-ASVspoof-`a/b/c` proposal (cannot populate the T60/Ds triplet; `a/b/c` is ASVspoof2019 *Physical Access* and adopting it would misattribute provenance). Pending: pull exact per-class T60/dimension ranges from the README or paper for the methodology paragraph.
- **2026-06-06** — **Hardened FishGram server health probe (false-negative abort fix).** The 12-cell production run (`--mode parallel`, 2026-06-01) finished 11/12; the lone failure was `fishgram/jittered` with `ConnectionError: Fish Speech API server is not reachable at http://localhost:8080` at step 2 (`cloner.load`). Root cause was NOT a downed server — Master Tomas confirmed via live logs the server served `POST /v1/tts 200` continuously throughout. The probe in `fishgram_attack/utils/cloner.py:load` did a single `GET /` with a 5 s timeout demanding an exact 200. In parallel mode both fishgram cells share one server on port 8080; while `not_jittered` was mid-generation (the GPU-bound `api_server` serialises requests on one worker), `jittered`'s liveness probe queued behind in-flight inference, exceeded 5 s, raised `requests.Timeout`, and — with no retry — aborted the whole multi-hour pipeline. `not_jittered` passed the identical check only because it owned the server at the time. Fix: `load()` now retries up to `FISH_SPEECH_HEALTH_RETRIES` (5) times with per-attempt timeout `FISH_SPEECH_HEALTH_TIMEOUT` (10 s) and `FISH_SPEECH_HEALTH_BACKOFF` (3 s) pauses, and treats ANY HTTP response (not just 200) as alive — only repeated ConnectionError/Timeout across all attempts is "down". Three new tunables added to `fishgram_attack/settings.py`. Re-run of the single `fishgram/jittered` cell deferred until the current run finishes. Open follow-up: in `--mode parallel`, both fishgram cells intentionally share one server, so the retry is now mandatory; alternative would be per-cell port pinning.
- **2026-06-09** — **Augmentation pipeline design refinements (3 decisions).** (1) RawBoost removed from offline corpus; moves to training-time on-the-fly augmentation (fast CPU op, no pre-baking benefit). (2) CODEC weight raised from 30% → 40% absorbing the freed 10%; new offline distribution: RIR_NOISE 60% / CODEC 40%. (3) Augmentation stacking introduced via a 3-way probability gate per clip: 60% single augmentation (RIR_NOISE or CODEC), 40% stacked (RIR_NOISE then CODEC in sequence). Stacked clips get a compound SYSTEM_ID using `|` separator (e.g. `RIR_MEDIUM_NOI_SNR12|CODEC_OPUS_BR16K_LOSS3PCT`); output directory structure unchanged (flat FLAC folder). Decision-log entry 2026-06-09 added; `methodology/data-augmentation.md` Section 2 and Section 8 updated.
- **2026-05-30** — **Per-file voiced-RMS loudness matching for spoof words.** Replaced silence-contaminated `normalize_energy` (bonafide reference was measured over the valley-snapped slot = word+silence, deflating it and leaving spoof words systematically too quiet) with new `app/pipeline/partial_spoof/utils/loudness.py` (`compute_voiced_rms` / `scale_to_reference_rms` / `apply_peak_ceiling`). One voiced-RMS anchor per utterance from the original bonafide host (silence-gated, adaptive gate `max(ENERGY_REFINE_SILENCE_RMS, 0.15*P95)`), shared by every spoof word in the file (W2/W3 consistency), computed before splicing so it is independent of `cumulative_offset`. Both splice branches now share one scaling helper (DRY). Peak ceiling 0.99 in Step 5 before `sf.write` guards against FLAC PCM_16 hard-clip in Step 7. New settings `LOUDNESS_MATCH_ENABLED/REFERENCE_MODE/VOICED_FRAME_MS/VOICED_GATE_FRACTION/PEAK_CEILING`; `splice_details` records `loudness_target_rms`/`loudness_applied_scale`. CPU-only verification at `app/tests/test_loudness_matching.py`. **First ml-server03 validation showed +2.37 dB overshoot** (clone scaled by plain RMS vs voiced anchor -> voiced content overshot). **Fixed** with `voiced_match_gain` (clone measured voiced, apples-to-apples; `scale_to_reference_rms` removed). Re-validated across 12 cells / 292 samples via new `scripts/check_loudness_match.py`: OVERALL spoof-vs-bonafide delta mean -0.14 dB, 93.5% within +/-1.5 dB, no clipping (peak <= 0.990). not_jittered cells 96-100% within tolerance; jittered cells slightly looser (79-90%) due to Step 5b boundary ops shifting boundaries off the measured timestamps (benign). openvoice 0 samples (SIM gate, known). Loudness matching ready to fold into the production re-run.

- **2026-05-25** — **Paper restart: verified bibliography + TASLP target.** Decision to write the IEEE paper from scratch via the academic-research-skills `/ars-plan` Socratic workflow, using the thesis-wiki as source of truth (archived 2026-05-10 draft demoted to reference-only). Ran `/ars-citation-check` over the archived draft: all 23 refs exist (none fully fabricated) but 9 needed correction. Created `IEEE/references.bib` (33 verified entries, each checked vs arXiv/DOI/ISCA). Corrected: `fishspeech`/`qwen3tts`/`cosyvoice` titles+years (invented subtitles), `parakeet` (fabricated report -> real TDT paper Xu et al. ICML 2023 arXiv:2304.06795 + model card), author errors in `huang2024detectable` (W.->S.-F.), `omnivoice` (K.->H. Zhu), `li2025hqmpsd` (J.->M. Li). Added missing `habla` (Tamayo-Florez/Manrique/Nunes Interspeech 2023 -- the bonafide corpus, advisor is co-author), Spanish/multilingual corpora (hispaspoof, speechfake, lrlspoof), detection baselines (aasist, rawnet2, tak2022ssl), augmentation (musan, ko2017rir), forced alignment (mfa). Flagged: HABLA paper describes 5 nations/~22k bonafide but thesis uses "HABLA v2" (7 accents/1,567 speakers) with no published citation -- must present v2 as our own extension. Wiki: anti-spoofing-datasets.md (HABLA entry + verified citations for HISPASpoof/SpeechFake/LRLSpoof), detection-methods.md (AASIST/RawNet2/wav2vec2-SSL citations), decision-log.md (2026-05-25 entry).

- **2026-06-01** — **Augmentation pipeline researched and documented.** Deep research pass (109 agents, 25 claims verified) on codec/loss augmentation parameters. Key findings: AMR-NB mandatory 4-mode set (GSMA IR.92), AMR-WB mandatory 5-mode set (GSMA IR.36), OPUS speech bitrates anchored by RFC 7587, codec augmentation reduces EER 50% (Cohen et al. 2022), packet loss must be burst-aware Gilbert-Elliott not Bernoulli (Bolot 1993). Codec deployment weights (opus 38% / amrwb 25% / amrnb 8% / ...) confirmed as engineering priors with no LATAM primary source. CODEC layer redesigned: real ffmpeg round-trip, tier×codec×mode hierarchy, PLC per codec, fringe-mode tail for AMR. New wiki page: methodology/augmentation.md. Decision log updated with 2026-06-01 entry.
- **2026-07-09** — **Corpus named MARSA; venue switched to Scientific Data (Data Descriptor); production status verified on-server; partial-spoof splice facts code-audited.** Paper-planning session via `/ars-plan`. (1) **Name:** corpus is now **MARSA** (Multi-accent Audio Recordings for Spanish Anti-spoofing), chosen over finalist FARSA; retire "HABLA-Spoof". Memory `project_marsa_naming.md` created. (2) **Venue:** Nature **Scientific Data**, Data Descriptor format (Background/Methods/Data Records/Technical Validation/Usage Notes/Availability) — SUPERSEDES the 2026-05-25 TASLP/IMRaD decision. Mandates public deposit (Zenodo/Figshare), confirmed acceptable. Baseline anti-spoofing EER is the one remaining blocker (model not yet chosen). (3) **Production verified 2026-07-09** by on-server FLAC counts: all 6 TTS pipelines COMPLETE — FishGram 34,197 / Qwen 31,568 / OpenVoice 29,626 / Chatterbox 31,701 / OuteTTS 25,642 / OmniVoice 33,743 = ~186,477 full spoof; partial spoof 86,766 across 6 attacks; ~35,927 bonafide; grand total ~309,170 utterances pre-augmentation; augmentation 2x/3x/5x/10x folders exist. Chatterbox/OuteTTS were still "RUNNING" and OmniVoice "validation-only" in the 2026-05-06 wiki — now corrected in production-runs.md. (4) **Literature grounding gathered:** Spanish dialect acoustic variation (Kantharuban 2023 EMNLP Findings; Zuluaga-Gomez 2023 Interspeech CommonAccent; Nacimiento-Garcia 2024 Applied Sciences) for the "Spanish is not one language" argument; prior Spanish partial-spoof HQ-MPSD (Li et al. 2025, ~13k Spanish samples, single TTS/accent, no augmentation) — MARSA exceeds it on every axis (6 systems, 7 accents, W1/W2/W3 tiers, 7 splice methods, boundary jitter, augmentation). 4 bib entries to add. (5) **Code-audited the partial-spoof splice** against source (`splice_method.py`, `crossfade.py`, `splice_engine.py`, `step_04_select_words.py`): confirmed exact `SPLICE_METHOD_WEIGHTS` (CUT_PASTE .10 / OLA_HANNING .20 / LINEAR .15 / COSINE .20 / HALF_SINE .15 / LOGARITHMIC .10 / PARABOLA .10) and fade curves; flagged that enum `HALF_SINE` is actually the square-root law `sqrt(t)` (misnomer — describe by math form in the paper); corrected the code-inaccurate 7-technique table in state-of-art/splicing-techniques.md (removed fictional per-method overlap ranges; single `[CROSSFADE_MIN_MS, CROSSFADE_MAX_MS]` draw). Wiki updated: production-runs.md, splicing-techniques.md, partial-spoof-approach.md, decision-log.md (2026-07-09 entry). Paper drafting begun.
- **2026-07-14** — **Partial-spoof clean subset, leakage re-verified, augmentation 2x smoke-tested, codec-availability finding.** (1) **Clean subset:** applied the full-spoof intelligibility gate (WER<=0.15 & CER<=0.10) to the 18,421 -> **15,641** (84.9%); losses ~9-18% per system (composition preserved). Copied to `data/partial_spoof_clean/` (audio + clean CSVs); full 18,421 kept intact. Paper reports 18,421 total + 15,641 recommended clean partition (deposit all, train on clean) across abstract/Table 1/Data Records/Usage Notes/Technical Validation. (2) **Leakage re-verified 2026-07-14** via `scripts/check_loudness_match.py` (needs `PYTHONPATH=.`): spoof-vs-bonafide loudness gap **mean -0.13 dB** over 18,164 samples, no clipping; 90.6% within +/-1.5 dB overall (non-jittered cells 96-100%; jittered cells 82-87% because Step 5b shifts boundaries off the measured timestamps -- measurement artifact, not a real offset). (3) **Author list:** Tomas Acosta Bernal, Juan Camilo Vasquez Correa (affiliation PENDING), Martin Daniel Calderon Acosta, Ruben Francisco Manrique (corresponding). (4) **Augmentation 2x smoke test (`--mode uniform` = Option B):** pipeline works (both classes, clean+aug, uniform loudness -23 dBFS) but the foreground run died on PuTTY disconnect at ~60% of spoof-aug -> use nohup/tmux. Fixed misleading logging: uniform mode no longer prints "Target ratio 50/50" (it prints "Class ratio: natural") and the output folder is now `augmented_2x_uniform_natural` not `_5050` (run_augmentation.py + augmentation_pipeline.py). (5) **CODEC AVAILABILITY FINDING:** the ml-server03 ffmpeg build provides only **3 of 6 codecs** -- G.711 mu-law/A-law + Opus enabled; **AMR-NB, iLBC, AAC SKIPPED** (not in build). Paper updated to state codecs are probed at runtime and the reference-build applied set is G.711+Opus, with AMR-NB/iLBC/AAC needing a non-default ffmpeg build. Open decision: (A) report the 3 that run, or (B) install a static ffmpeg with libopencore-amrnb+libilbc to run all 6. (6) Added 2 presentation slides (15h corpus-verified, 15i leakage-confirmed). (7) Notion MCP added to `.mcp.json` (pending OAuth). Committed on branch feat/augmentation.
- **2026-07-14** — **OpenVoice watermark-free re-run complete + authoritative corpus counts corrected.** OpenVoice regenerated clean (29,796 passed, WER 1.50% / NISQA 4.38 / SIM 0.388 -- identical to watermarked within noise, confirms watermark removal is quality-neutral). Partial-spoof re-aggregated. **Major count correction:** partial spoof is **18,421** spliced samples (from `corpus_samples.csv`), NOT 86,766 -- the latter was a raw `find *.flac` miscount that counted intermediates (cloned/spliced/references/LA ~4.7x). Full-spoof counts re-verified authoritative via LA/ FLAC (only OpenVoice changed: 29,626->29,796); total full spoof 186,647; corpus base total **240,995** (was mis-stated 309,170). Filled Chatterbox (WER 1.20/NISQA 4.39/SIM 0.714) + OuteTTS (2.36/4.45/0.462) metrics from metrics CSVs -> removed those main.tex PENDINGs. **OmniVoice metric correction:** production means NISQA 4.28 (LOWEST, not 4.59 highest -- 4.59 was 6-sample validation), SIM 0.686, WER 1.06%; fixed the false "OmniVoice highest quality" claim. main.tex updated: Table 4 (all 6 real metrics + total), abstract/Table 1/contributions counts, HQ-MPSD reframed from "6-7x larger" to comparable-scale/diversity-led (18,421 vs ~13k HQ-MPSD = 1.4x), added partial-spoof composition table (tab:pscomp), ECAPA-gate disclosure now cites OpenVoice=53/18,421, quality_flag distribution added. production-runs.md summary table + MEMORY.md attack status rewritten authoritative.
- **2026-07-13** — **Paper review passes (citation-check + methodology-focus + domain/literature) on main.tex, fixes applied.** (1) `/ars-citation-check` (Opus): 39 refs verified, nothing fabricated; fixed speechfake authors (W. Huang/Y. Gu, was Y. Huang/J. Gu), filled hispaspoof (Risques/Bhagtani/Yadav/Delp, ICASSP 2026) + lrlspoof authors. (2) Methodology-focus review (EIC + methodology, Opus) -> Major Revision; Bucket A writing fixes applied to main.tex (parameter table `tab:params` closing reproducibility C3, speaker-vs-utterance conflation, "order of magnitude"->"6-7x" overclaim, ECAPA-0.60-gate disclosure re OpenVoice absent from partial spoof, full-spoof gating reconcile WER<=0.15/CER<=0.10, Ethics Declaration section, m4/50:50->Option B uniform-augmentation claim). (3) Domain/literature review (Opus): CRITICAL -- HISPASpoof (closest prior art, Spanish 6-accent 6-TTS) was uncited and contradicted the "Spanish void" framing; MLAAD + SpeechFake (multilingual, include Spanish) omitted; ADD 2022 + ASVspoof 5 missing; HQ-MPSD wrongly claimed "no acoustic augmentation" (it has clean+noisy variants); dangling `\citep{mfa}` -> compile [?]. FIXED: added 6 bibitems (asvspoof5, add2022, hispaspoof, mlaad, speechfake, mfa) with matching cites, reframed Background to acknowledge neighbors and carve MARSA's niche (cross-continental + full+partial + jitter + augmentation), corrected HQ-MPSD claim, repaired mfa compile bug, softened "overwhelmingly"->"predominantly". 3 new refs (mlaad/add2022/asvspoof5) synced to references.bib. Open: advisor question on t-DCF+DET (not committed to paper); Bucket B measurements remain tech debt gated on data finishing. Wiki: [advisor-questions], [paper-review-techdebt] memories. HISPASpoof author list has a cross-agent discrepancy -- confirm vs arXiv before submission.
- **2026-07-09** — **OpenVoice WavMark watermark contamination found and fixed.** Code audit of all six cloners revealed OpenVoice V2 embeds a WavMark steganographic watermark (`@MyShell`) in the waveform by default (`ToneColorConverter` defaults `enable_watermark=True`; `convert()` calls `add_watermark()` unconditionally). All ~29,626 OpenVoice full-spoof samples were watermarked -- a label-leak shortcut (detector could learn the watermark, not the synthesis artifact). WavMark survives resample/re-encode. Fixed with `enable_watermark=False` in `openvoice_attack/utils/cloner.py` (mirrors Chatterbox's `NoOpWatermarker`). Master Tomas's memory had it inverted (thought Chatterbox's kept / OpenVoice's unremovable); verified against OpenVoice `api.py` source. Decision: regenerate ALL OpenVoice (full + partial spoof) with the fixed cloner. Also documented the previously-undocumented per-system pre/post (FishGram text-normalize, Qwen/FishGram hallucinated-prefix trim, OpenVoice VAD tone-embedding extraction, Chatterbox trailing-noise VAD trim, OuteTTS speaker profile, OmniVoice ref-silence + RMS prefix reject). Wiki: decision-log 2026-07-09 entry, attack-systems.md new "Per-system pre/post-processing and watermarking" section; memory [openvoice-watermark-contamination].
- **2026-07-09** — **Two manuscripts drafted + Tesis 2 scoped.** Created `IEEE/main.tex` (MARSA Scientific Data Data Descriptor: English, self-contained single file with embedded bibliography for one-paste Overleaf compile; sections Background & Summary / Methods / Data Records / Technical Validation / Usage Notes / Code & Data Availability; baseline-EER, Chatterbox/OuteTTS metrics, author + Zenodo DOI marked PENDING in red). Created `IEEE/thesis.tex` (Tesis 2 proposal: Spanish, 9-section Uniandes format, author = Tomas Acosta Bernal / asesor Ruben Manrique). Tesis 2 = evaluation continuation: augmentation at scale + partial-spoof augmentation + fine-tune 3 modern 2025-26 detectors (Nes2Net, DF_Arena/RAPTOR, HoliAntiSpoof) + stratified in-corpus EER + cross-corpus eval (train MARSA, test HISPASpoof) + submit the Data Descriptor. One semester (~16 wks). `IEEE/references.bib` grew to 39 entries (+3 dialect: Kantharuban/Zuluaga-Gomez/Nacimiento-Garcia; +3 detectors: nes2net arXiv:2504.05657, dfarena arXiv:2603.06164, holiantispoof arXiv:2602.04535). Both document titles still pending (Master Tomas wants a partial-spoof + augmentation framing). Style pass on thesis.tex: removed AI-tell em-dashes (rhetorical `---` -> parentheses/colon; numeric ranges kept) and stripped low-level impl detail (CUDA_VISIBLE_DEVICES, ml-server03 hostname, venv, ffmpeg) that a committee will not see. Decision-log 2026-07-09 entries (MARSA/venue + Tesis 2 scope) added. See memory [project-tesis2-scope], [project-marsa-naming].
- **2026-06-01** — **HABLA v2 accent inventory verified on ml-server03 -- corpus is CROSS-CONTINENTAL.** Authoritative `find` over `data/bonafide_dataset_by_speaker_v2` (1,567 speaker dirs, 35,927 audio files) shows 7 accent codes: es=528 (Spain/Peninsular, LARGEST at 33.7%), mx=406, co=357, cl=173, ar=42, pe=38, ve=23. KEY FINDING: HABLA v2 is NOT Latin-American-only -- European/Peninsular Spanish is the plurality accent. European Spanish becomes a CONTRIBUTION (cross-continental scope), not future work. Corrected three wrong records: (1) auto-memory said "7 Latin American accents (ar,co,mx,pe,cl,ve,cu)" -- no Cuba, ignored Spain; (2) presentation slides 00b/00c/12_hispaspoof claim "7 LatAm" + "we include Venezuelan and Puerto Rican; HISPASpoof has Peninsular" -- all false (no cu/pr codes; HABLA has more Peninsular than HISPASpoof); (3) `app/cloner/transcriber.py` country_map maps only 5 countries (es/mx -> "unknown", latent bug). Flagged severe accent imbalance (es+mx+co=82%; ar/pe/ve=6.6%) for Limitations, and likely Mozilla Common Voice provenance (generate_cv_graphs.py 382:1 ratio). Full inventory table written to state-of-art/anti-spoofing-datasets.md. Slides still need correcting before external use. Earlier in session (claude was wrong, user was right): had asserted Spain absent based on stale local v1 (162 speakers) + incomplete transcriber map.
- **2026-08-01** — **Two silent data-loss bugs killed the first two real 2x augmentation runs.** Run 1: dev/eval empty (loader wanted `val`/`test` dir names; legacy fallback misread `train`/`dev`/`eval` as "3 speakers"). Run 2: class ratio 90/10 instead of the true ~85/15 -- `.mp3` filtered out of discovery, dropping ALL 13,111 Common Voice bonafide (36.5% of bonafide; zero spoof affected). Fixes: single `_discover_split_files()` path (was triplicated -- the reason each bug survived the other's fix), `.mp3` accepted, speakers sorted (seed reproducibility), `DatasetDiscoveryReport` schema (every on-disk file ingested or counted), `_preflight_check()` aborts in ~2 min on empty splits / ignored files / undecodable extensions instead of producing a silently wrong corpus after 1-2 h. Both wrong 2x outputs discarded. Verified rerun: exact symlink counts, bonafide 15.1/15.1/14.6%.
- **2026-08-02** — **Codec backend fixed (3/6 -> 6/6) and leak audit closed.** Bare `except` had masked: AAC container bug (`adts` write-only -> `mpegts`), AMR-NB wrong encoder name + absent from stock Ubuntu ffmpeg (now `libopencore_amrnb` via pinned static binary `tools/ffmpeg-7.0.2-amd64-static/ffmpeg`, env `MARSA_FFMPEG_BINARY`, subprocess backend, 41 ms/clip), iLBC unobtainable everywhere -> replaced by Speex (same legacy-VoIP slot, in-process). Realized mix verified within 0.2pp of all six targets; AMR-NB/Speex spectrally confirmed (zero energy >4 kHz). Leak audit: audio-ID ordering leak CONFIRMED on 2x (bonafide ids 1..54,266 contiguous) -> shuffled ID pools + ID-sorted rows; mtime emission-order leak -> constant `CORPUS_MTIME`; loudness/duration/clean-fraction/protocol-file-correspondence/speaker-disjointness all verified clean. Protocol format finalized: byte-exact ASVspoof2019 LA 5-field file + `MARSA.LA.cm.<split>.metadata.csv` for the augmentation label; ATTACK_ID slugs enable LOSO (all 6 systems present in every split). MP3=CV provenance proven per accent (es/mx 100% mp3, ar/pe 100% wav, 0 mixed speakers, total exactly 13,111); "Channel provenance" limitation added to main.tex Usage Notes. Open: phrase-overlap and cross-split voice-similarity checks pending; 2x/3x/5x/10x regeneration pending (all prior outputs invalid).
- **2026-08-03** — **Split-design leak audit closed; partition rebuilt; 2x validated; 3x/5x/10x launched.** (1) Voices: full-coverage cross-split ECAPA audit (1,567/1,567 speakers) found 6 pairs > 0.75 (max 0.901, near-certain same-person HABLA session duplicates); the 4 dev/eval members consolidated into train (`SIMILARITY_FORCED_TRAIN`); rebuilt partition (1,257/155/155 speakers; all source totals exact: 35,927 / 186,647 / 15,641); re-audit CLEAN, residual maxima 0.741/0.734/0.620. (2) Phrases: sentence overlap measured via the partial-spoof manifest -- 66.0% of eval and 53.1% of dev utterances reuse a train sentence; canonical split kept (label-independent; field parity with ASVspoof2019), strict sentence-disjoint eval filter list approved, `source_file` column added to metadata CSV to make it joinable. (3) 2x rerun on rebuilt partition: all reconciliations exact; 409 stale orphan FLACs from the superseded run discovered via an mtime-count discrepancy, quarantined; preflight now refuses dirty output dirs. Verification battery all green (ID interleaving 304,797; windows 14.6-15.7%; 1 mtime; codec mix 6/6 within 0.3pp; per-class family mix identical to 0.1pp; 25 attack ids; source_file complete). (4) 3x/5x/10x launched in two tmux sessions (10x alone + 3x/5x chained; ~50 GB RAM audio cache per process bounds parallelism on the shared server).
- **2026-08-04** -- **Corpus complete: all four augmentation factors generated + verified; strict eval filter final.** 3x/5x/10x battery green (exact counts, interleaving, single mtime); 10x mtime reconciliation exact (guard prevented orphan recurrence). Strict filter after three verified fixes (CV parser, OuteTTS ordering, two-pointer duration matching): eval 74.2% strict (bonafide 32.3 / full 91.2 / partial 33.2), 4.9% conservatively unresolved; bonafide rate cross-validates the measured sentence overlap (33.6 predicted). Hours measured: 225.2/38.2/34.5 source -> Table 1 totals 523/748/1,199/2,325 h. Paper Table 1 + strict-filter paragraph finalized.
- **2026-08-24** -- **Tesis 2 starts: detector training pipeline built, with a committed corpus audit gating the GPU.** (1) Context recovered: wiki log ended 2026-08-04 (corpus complete, four tiers verified, strict filter final); the 08-09/08-15 commits (ICAI speaker-leakage scripts, `IEEE/jaime.tex`) were never logged. (2) Server recon: Jaime's HABLA v1 LCNN checkpoints DO exist (`03-asvspoof-mega/trained_network{,_lstm,_att,_fix,_finetune_att,_finetune_lstm}.pt`, Xin Wang's project-NN-Pytorch-scripts, env `~/ambientes/speech`, python3.10) -- so the zero-training baseline is runnable; BUT `DATA_LAT`/`DATA_UN` hold only `asvspoof2019_LA/protocol.txt` and an UNEXTRACTED `latam.rar`, no train/val/test audio, so the ICAI leakage scripts' path assumption is wrong and the baseline needs the audio restored first. All 4 A40s idle; 212 GB free of 6.9 TB (97%); no huggingface_hub in system python; HF reachable. (3) Decisions: train 2x tier first, DF_Arena_1B first detector. (4) Built `app/pipeline/training/` -- ONE shared, backend-agnostic harness (Facade + 5 steps + schemas + settings + utils) with each detector as a subpackage inside it (`training/dfarena/` first; `lcnn/`, `nes2net/`, `holiantispoof/` sit beside it). Copying the harness per detector was rejected as a DRY violation and `app/scripts/{run_detector_training,run_corpus_audit}.py`. Step 1 is a hard audit gate: 8 invariants from text files only, ~2 min, no GPU, writes citable `corpus_audit.json`. Full rationale in decision-log 2026-08-24. Auditor VERIFIED against synthetic corpora (clean fixture passes 8/8; fixture with planted cross-split speaker + orphan FLAC + class-by-class ID ordering fails exactly those 3, names offenders, exits 1). An import smoke test caught and fixed a real defect: eager `__init__.py` re-exports made the audit pull in torch/transformers/soundfile, defeating its no-GPU design; all `__init__.py` in this package now re-export nothing. (5) Baseline plan clarified with Master Tomas -- 2x2 grid {classic LFCC-LCNN, SOTA DF_Arena} x {zero-shot, trained-on-MARSA}; his weights on our eval is a TRANSFER test (do existing detectors handle 2025-era cloners?), his architecture trained on MARSA is the IN-DOMAIN test the descriptor needs. Correction: scoring his checkpoints needs only weights + front-end code, NOT the HABLA v1 audio -- only the leakage verification needs that, and `DATA_LAT/asvspoof2019_LA/protocol.txt` exists so it is likely recoverable without extracting latam.rar. OPEN: eval-only mode not implemented (needed for both zero-shot cells); LFCC-LCNN backend not written; confirm DF_Arena's native head vs the bolted-on pooled head before the first full run; `envs/dfarena_env` not yet created.
- **2026-08-24 (cont.)** -- **DF-Arena interface probed; eval-only mode added; audit env corrected.** (1) `AutoConfig` probe of `Speech-Arena-2025/DF_Arena_1B_V_1` returned: `auto_map.AutoModel = modeling_antispoofing.DF_Arena_1B_Antispoofing`, a dedicated `AutoFeatureExtractor`, `out_dim: 1024`, `sample_rate: 16000`, `num_labels: 2`, `id2label {1: bonafide, 0: spoof}`. Three consequences: `AutoModel` IS the right class and the label order matches ours; the model ships a NATIVE two-class head that the adapter was discarding in favour of a freshly initialised one; the feature width lives in `out_dim`, NOT `hidden_size`. The adapter's old silent fallback to a hard-coded 1024 happened to be correct for this checkpoint -- accidental correctness that would have masked the bug on any other backbone. Replaced with `_feature_dim()` reading `out_dim`/`hidden_size`/`output_hidden_size` and RAISING rather than guessing. Full rewrite of `DFArenaDetector.forward` is blocked on reading `modeling_antispoofing.py` (the published feature extractor implies it does not take a raw `input_values` tensor). (2) **Eval-only mode implemented** (`--eval-only`, `--eval-checkpoint`): facade branches to `_evaluate_only`, no optimiser is built and the training split is never read -- this is what both zero-shot cells of the baseline grid require. (3) Corpus audit could NOT run in `fishgram_env` (`No module named loguru`); per the pinned-env rule nothing was installed there -- the audit runs from `dfarena_env` instead, which already carries loguru and pydantic. (4) Confirmed Block A: `data/augmented/` holds `augmented_{2x,3x,5x,10x}` so the `settings.CORPUS_ROOT` default was already correct; `strict_eval_filter.csv` present (4.0 MB, Aug 4). (5) Jaime's checkpoint confirmed LFCC-LCNN in the project-NN-Pytorch-scripts layout: flat 61-tensor state dict, `m_frontend.0.lfcc_fb (257,20)` (20 filters over a 512-point FFT), `m_frontend.0.l_dct.weight (20,20)`, `m_transform.*` conv stack with Max-Feature-Map halving (conv out 64 -> BN 32), plus `input_mean/std` buffers. LCNN backend still blocked on his `model.py`/`config.py`. OPEN: corpus audit on the real 2x tier NOT YET RUN (code had to be pushed/pulled first).
- **2026-08-25** -- **CORPUS AUDIT PASSED on augmented_2x; tier cleared for training; citable artefact now exists.** First real run of `app.scripts.run_corpus_audit` (from `dfarena_env`, ~7 s wall clock, no GPU). All five FATAL invariants green, three non-fatal green. Report written to `data/augmented/augmented_2x/corpus_audit.json` -- this is the reproducible artefact the Scientific Data Technical Validation section previously lacked for every leakage claim. Results: rows train 359,198 / dev 30,808 / eval 27,808; speakers 1,257/155/155 with ZERO cross-split overlap (matches the rebuilt partition exactly); protocol-vs-metadata exact on all three splits (0 protocol-only, 0 metadata-only, 0 field mismatches); on-disk exact (0 missing, 0 orphans -- the dirty-output guard held); clean-fraction parity train bonafide 0.5000 vs spoof 0.5000, gap 0.0000 (dev/eval are 1.0000 both classes, i.e. UNAUGMENTED, confirming the Layer-1-only design and vindicating the choice to check PARITY rather than assert zero augmentation); audio-ID ordering run-ratios 0.999/0.999/1.004 (no ordering leak); class balance 0.1514/0.1512/0.1464; strict-filter join dev 76.5% / eval 74.2% -- the eval figure is IDENTICAL to the 2026-08-04 recorded value, an independent cross-validation of both the filter and the tier. Source totals reconcile exactly: 179,599 + 30,808 + 27,808 = 238,215 = 35,927 bonafide + 186,647 full spoof + 15,641 partial-spoof CLEAN subset (not the 18,421 deposited). TWO ITEMS TO RESOLVE: (a) the audit reports **7** attack-system families but `EXPECTED_ATTACK_SYSTEMS` lists only 6 -- an unexpected extra family passes silently because the check only fails on ABSENT systems; identity of the 7th unknown, must be resolved before any per-attack table is published; (b) eval carries **22** attack ids against 24 in train/dev, so two ids (likely partial-spoof tier variants) have no eval clips, which constrains leave-one-attack-out at id granularity though not at system granularity. Also confirmed: Jaime's `config.py` is the stock Xin Wang project-NN baseline config (raw waveform input `input_dims=[1]`, `wav_samp_rate=16000`, `input_exts=['.wav']`, `truncate_seq=None`); his `model.py` is 431 lines, not yet read.
- **2026-08-25 (cont.)** -- **Corpus naming DEFECT found: Qwen ships under two different attack_id names.** Resolved the "7 systems vs 6 expected" flag from the audit by dumping the attack_id inventory. Full spoof uses `qwen`; partial spoof uses `qwen3tts-psw{1,2,3}`. Every other system is consistent across both attack types (`omnivoice`/`omnivoice-psw1`, `chatterbox`/`chatterbox-psw1`, ...). CONSEQUENCES: (a) a per-attack table lists Qwen TWICE as if it were two systems; (b) leave-one-system-out is broken for Qwen -- holding out `qwen` leaves every Qwen partial spoof in training; (c) the inconsistency is in the metadata CSVs of the DEPOSITED corpus, so it reaches users unless fixed at source. Attack id count now reconciles: 24 = 6 full + 18 partial (6 systems x 3 tiers). SECOND FINDING: OpenVoice partial spoof has essentially no statistical power -- `openvoice-psw1/2/3` are train 40/32/16, dev 1/1/1, eval 1/0/0. This is the downstream consequence of OpenVoice's ECAPA SIM 0.388 against the 0.60 clone gate, and it explains eval carrying 22 attack ids against 24 in train/dev (openvoice-psw2 and -psw3 have zero eval clips). A per-attack EER on one eval clip is not a number; the evaluator must suppress rather than report those cells, and Usage Notes needs a sentence. THIRD: Jaime's `model.py` read in full -- LFCC-LCNN-BLSTM with P2SGrad. Front-end `LFCC(frame_len=320, hop=160, fft=512, sr=16000, dim=20, with_energy=True)`, deltas on (effective 60); LCNN conv stack with MaxFeatureMap2D and Dropout(0.7); two `BLSTMLayer(96, 96)`; `Linear(96, 64)`; `P2SActivationLayer(64, 2)` (cosine-margin, not softmax). Inference returns `out_score[:, 1]` and `protocol_parse` maps bonafide->1, so HIS SCORE CONVENTION MATCHES OURS (higher = more bonafide) -- no sign flip needed. No input normalisation is actually applied (`input_mean`/`std` are zeros/ones and `normalize_input` is never called in `forward`). Pooling is a residual SUM over frames, so his score is length-dependent by design. His `forward(x, fileinfo)` reads labels from a protocol file and is unusable directly; `_compute_embedding` + `_compute_score` are the parts an adapter wraps. LCNN backend still blocked on four vendored components: `MaxFeatureMap2D` and `BLSTMLayer` (sandbox/block_nn.py), `LFCC` (sandbox/util_frontend.py), `P2SActivationLayer` (core_modules/p2sgrad.py).
- **2026-08-25 (cont. 2)** -- **Qwen rename APPLIED and verified; DF-Arena adapter rewritten against the real published interface.** (1) `normalize_attack_ids --apply` rewrote **42,606 rows** across 25 artefacts (4 tiers x 3 splits x 2 files + strict filter), verification clean, mtimes preserved, `.bak` per file. Dry run reconciled three ways before applying: row totals summed exactly; train rows scaled 1,962/2,943/4,905/9,810 = 981 x {2,3,5,10}, independently proving all four tiers were generated consistently; dev (154) and eval (220) identical across every tier, confirming dev/eval are unaugmented; strict filter 374 = 154 + 220. Re-audit: **`attack_coverage` now reports 6 systems** in all three splits (was 7), all eight checks still PASS. (2) DF-Arena published source read (`modeling_antispoofing.py`, 21 lines; `feature_extraction_antispoofing.py`, 35 lines). THE MODEL IS A COMPLETE DETECTOR, not an SSL backbone: `forward(input_values, attention_mask=None)` takes a raw waveform and returns `{"logits": logits}` -- a PLAIN DICT, so the old `outputs.last_hidden_state` would have raised AttributeError, and the pooled head the old adapter bolted on would have discarded a trained output layer. `attention_mask` is accepted but never forwarded to the backbone. The feature extractor pins input to **exactly 64,600 samples** (4.0375 s at 16 kHz), truncating longer clips and TILING shorter ones -- which is what `MarsaAudioDataset._crop` already does. Adapter rewritten: no pooling, no classifier, no masking, `outputs["logits"]`, single optimiser group at the backbone rate (there is no fresh head to warrant the larger head rate), and the published label order `{1: bonafide, 0: spoof}` ASSERTED at construction rather than assumed, since a silent mismatch inverts every score. New `BaseSpoofDetector.required_samples` lets a backend declare its input contract; trainer and evaluator crop to it and log the override. (3) LCNN components received: `MaxFeatureMap2D`, `BLSTMLayer`, `P2SActivationLayer`, `P2SGradLoss`. STILL MISSING for the LCNN backend: the tail of `LFCC.forward` (util_frontend.py past line 280 -- DCT, energy replacement, delta, num_coef) and `LinearDCT` from `sandbox/util_dsp.py`. OPEN: `backbone.py` / `conformer.py` not read, so head-only fine-tuning of DF-Arena cannot be offered yet; whole-model fine-tuning at the backbone rate is what the adapter supports today.
- **2026-08-25 (cont. 3)** -- **DF-Arena architecture read; batch-size-1 limitation found and bypassed.** Model loaded successfully after two environment traps: (a) the remote code needs `einops` (conformer backbone) -- installed in `dfarena_env`, now pinned in the pipeline README; (b) the first failed load left `~/.cache/huggingface/modules/transformers_modules/Speech_hyphen_Arena_hyphen_2025` half-populated (`backbone.py` cached, `conformer.py` never fetched because the recursive relative-import walk aborted on the einops check), so the retry died on FileNotFoundError; fix is to delete that module-cache subtree, which does not touch the 4 GB of weights. Both documented in the README. **Architecture**: DF_Arena_1B = wav2vec2-XLS-R-1B encoder (`output_hidden_states=True`, 25 layers, D=1280) -> learnable per-layer attention pooling (`attn_scores` -> softmax over time -> weighted sum) -> `fc0` + sigmoid produces one scalar weight per layer -> layer features fused by that weighting and summed -> BatchNorm2d(1) + SELU -> `FinalConformer` (4 ConformerBlocks, 4 heads, kernel 31, sinusoidal positional embedding, prepended learnable class token) -> `fc5` on the class token -> 2 logits. **THE LIMITATION**: exactly one line, `out_ssl = self.ssl_model(x.unsqueeze(0))`, assumes `x` is a single waveform of shape (T,); handed (B, T) it yields (1, B, T) and wav2vec2's conv1d front end rejects the resulting 4-D tensor (`RuntimeError: Expected 2D or 3D input to conv1d, but got [1, 1, 16, 64600]`). Everything downstream is already batch-general: `get_attenF1D` reduces over `dim=1` of (B,T,D), and `FinalConformer` iterates the batch to prepend its class token. **FIX**: `DFArenaDetector.forward` now mirrors `DF_Arena_1B.forward` operation for operation with genuinely batched input, skipping only that unsqueeze -- same weights, same arithmetic, no batch-size-1 penalty over ~30k eval clips. Equivalence is MEASURED, not asserted: `app/scripts/verify_dfarena_batching.py` runs the same waveforms through both paths (`published_forward` loops the published wrapper one clip at a time) and compares logits and derived scores against a tolerance. `_assert_backbone_shape()` fails loudly at construction if a future model revision renames the submodules the batched forward reaches into. **CAVEAT for the methods section**: `first_bn` is a BatchNorm2d; in eval mode it uses running statistics so batching cannot change a single score, but fine-tuning with batch > 1 normalises across the batch rather than over one sample as the published training presumably did.
- **2026-08-25 (cont. 4)** -- **FIRST BASELINE NUMBER: DF-Arena zero-shot on MARSA dev.** Batching equivalence verified first (max |logit diff| 1.161e-04, max |score diff| 2.284e-04 against a 1e-3 tolerance -- float-kernel noise, nothing structural). Model: 1,147,845,253 trainable parameters, 966/966 shards. **dev pooled EER 10.667% over 30,808 clips; strict EER 8.553% over 23,583 clips.** Per-attack, full spoof: openvoice 0.000% (n=3,822), outetts 0.189% (n=3,238), chatterbox 2.428% (n=4,113), omnivoice 3.501% (n=4,452), fishgram 4.359% (n=4,449), **qwen 21.569% (n=4,099)**. Partial spoof, every tier: 25-44% (omnivoice-psw1 43.532% n=758, fishgram-psw1 41.381% n=58, omnivoice-psw2 39.832% n=371, chatterbox-psw1 37.341% n=201, omnivoice-psw3 37.371% n=99, outetts-psw1 34.257% n=50, qwen-psw1 34.144% n=85, chatterbox-psw2 33.592% n=128, fishgram-psw2 32.262% n=40, outetts-psw2 30.560% n=36, chatterbox-psw3 29.510% n=50, qwen-psw2 28.430% n=49). **FINDING 1: attack quality predicts detectability across all six systems** -- Qwen is 5-100x harder than the rest and has the HIGHEST ECAPA SIM (0.720) and LOWEST WER (1.46%); OpenVoice is perfectly separable at 0.000% and has the WORST SIM (0.388). **FINDING 2: partial spoof is near-chance for a SOTA 1B detector that dismantles full spoof.** **FINDING 3:** the low-sample-count flags proved their worth immediately -- openvoice-psw1 88.635% and openvoice-psw3 18.362%, same attack, 70 points apart, both n=1. **TWO CAVEATS TO RESOLVE BEFORE PUBLICATION.** (a) **The strict EER does not measure leakage for a zero-shot model.** 8.553% is LOWER than the pooled 10.667% because DF-Arena never trained on MARSA, so removing sentences overlapping OUR train split removes no leakage -- it only changes test composition, and per the audit the strict subset retains 91% of full spoof against 33% of partial spoof and 32% of bonafide, i.e. it is enriched in the easy class. The strict figure is interpretable only for models TRAINED on MARSA; reporting it for zero-shot runs without this caveat would be wrong. (b) **The 64,600-sample centre crop may be inflating the partial-spoof numbers.** Evaluation takes a centre crop of 4.0375 s. For any utterance longer than that whose spliced word sits near the start or end, the crop EXCLUDES the only spoofed audio, so the model is shown genuine speech and asked to call it spoof -- unwinnable by construction. Some of the 25-44% is real difficulty, some may be an artefact of our own scoring window. "Partial spoof defeats SOTA detectors" must NOT be claimed until the split is measured. Planned: (i) measure directly what fraction of recorded splice boundaries fall outside the centre crop, using the partial-spoof pipeline's boundary metadata; (ii) implement sliding-window scoring over the full utterance with max spoof evidence, which is standard practice for utterances longer than the model's input contract.
- **2026-08-25 (cont. 5)** -- **LCNN backend built for Jaime Hurtado's ICAI-2026 baseline (LFCC-LCNN-BLSTM-P2SGrad).** Module-for-module port from project-NN-Pytorch-scripts (Xin Wang, NII): `MaxFeatureMap2D`, `BLSTMLayer`, `LFCC` (filterbank+DCT+energy+delta), `LinearDCT`, `P2SActivationLayer` -- each faithfully reproducing the published source, one class per file per CLAUDE.md. New `app/pipeline/training/lcnn/` backend registered in `DetectorFactory` alongside `dfarena`. Two deliberate, documented deviations from the literal source: (1) `stft_wrapper` uses `torch.stft(..., return_complex=True)` unconditionally instead of the original's torch-version branch on the legacy `[...,2]` layout, reading amplitude via `.abs()` instead of `torch.norm(...,-1)` -- mathematically identical (modulus of a complex number IS the L2 norm of its real/imaginary pair), just not version-conditional; (2) `LinearDCT` does not reimplement the closed-form DCT-matrix derivation (`dct()`/`dct1()`/`idct()` in util_dsp.py) since every use here loads a PRETRAINED checkpoint where `l_dct.weight` is already baked in -- reconstructing that math would be pure risk for zero benefit. **A real correctness issue was caught and closed before it could produce silently-wrong scores**: unlike DF-Arena, LCNN's own pooling step (`(hidden_features_lstm + hidden_features).sum(1)`) is unmasked in the source it was ported from, correct only when every clip in a batch shares one length. Deriving a frame-count-through-four-poolings mask analytically was judged too fragile to trust without server-side testing, so `forward()` instead asserts uniform batch lengths and RAISES rather than silently summing padded frames as if they were signal -- verified this fires correctly in the shape-trace test below. **Verified locally (CPU, no checkpoint, random init) before ever touching the server**: LFCC frontend produces the expected (B, frames, 60) feature (20 base + energy-replaced-coef-0 + delta + delta-delta); full forward pass through frontend->conv stack->2xBLSTM->P2SGrad runs end-to-end with zero shape errors -> (B,2) logits; parameter count 281,512 total / 275,968 trainable, and the 5,544 frozen difference reconciles EXACTLY against `lfcc_fb (257x20) + l_dct.weight (20x20) + 4 legacy input/output mean/std buffers` -- an independent cross-check that the Sequential index derivation (verified earlier against the checkpoint's own parameter shapes, m_transform.0.{0,3,5,6,9,10,12,13,...}) is correct; uniform-length guard confirmed to raise on a mismatched-length batch. Checkpoint choice: `trained_network_lstm.pt` (LSTM-sum pooling backend) -- the ONLY one of his six checkpoints whose architecture matches the model.py already read; `_att`/`_fix` variants use a different `m_before_pooling` and would need a different model.py, not yet read. Score convention already matches ours (index 1 = bonafide) per the earlier read of `protocol_parse`/`_compute_score`, no relabelling applied. STILL UNVERIFIED, needs the server: does the real checkpoint's state dict actually `load_state_dict(strict=True)` without a key/shape mismatch. Crop-confound measurement (measure_crop_confound.py, written last session) still not run against the real corpus -- deprioritized this session in favor of the LCNN backend per Master Tomas's direction ("we should run jaime's models").
- **2026-08-25 (cont. 6)** -- **finetune_lstm zero-shot run; CRITICAL checkpoint/architecture filename swap discovered and resolved; fixed-crop LCNN backend built.** (1) `trained_network_finetune_lstm.pt` scored (free win, zero new code, just a different checkpoint path): dev pooled EER 40.951%/strict 39.173%, eval 35.291%/34.697% -- 5-6pp better than the non-finetuned lstm checkpoint (46.0%/56.1% dev, 40.9%/59.5% eval) AND the strict/pooled relationship FLIPPED to match DF-Arena's pattern (strict now lower than pooled, unlike the base lstm checkpoint). (2) `trained_network.pt` resolved: its 61-key state dict is structurally IDENTICAL to `trained_network_lstm.pt` (same BLSTM x2 + Linear(96,64) layout) -- confirmed a duplicate, not a 4th distinct architecture. Only 3 backends exist, matching the ICAI paper's "3 LCNN back-ends" (lstm-sum [done], attention/SelfWeightedPooling, fixed-crop). (3) **MAJOR FINDING: the checkpoint FILENAMES on ml-server03 do not match their architectures.** Read both `lfcc-lcnn-attention-p2s/01/model.py` and `lfcc-lcnn-fixed-p2s/01/model.py` in full and cross-matched every dimension against the real checkpoint shape dumps: the `lfcc-lcnn-attention-p2s` directory (SelfWeightedPooling, variable-length, no truncation) produces `Linear(192,64)` + `m_pooling.mm_weights(1,96)` -- EXACTLY matching `trained_network_fix.pt`'s shapes, not `trained_network_att.pt`. The `lfcc-lcnn-fixed-p2s` directory (750-frame hard truncation, random-crop-at-eval-time, flatten+Dropout+Linear(4416,160)+MFM+Linear(80,64)) EXACTLY matches `trained_network_att.pt`. The filenames are swapped relative to Xin Wang's own experiment-directory naming. Had this not been caught by matching real dimensions rather than trusting filenames, the wrong architecture would have been built against each checkpoint -- a `load_state_dict` shape crash at best, a coincidental-fit silent wrong-score risk at worst. Every new backend in this codebase is now named for its ARCHITECTURE, not for whichever checkpoint filename Jaime happened to save it under, with the mismatch documented explicitly in the settings docstring so nobody "fixes" it back to the wrong pairing later. (4) Built `app/pipeline/training/lcnn_fixedcrop/` (`LCNNFixedCropDetector`, default checkpoint `trained_network_att.pt`): reuses the LFCC frontend and MFM conv stack unchanged, adds a faithful port of the published `_front_end` frame-truncation override -- true frame count estimated as `waveform_length // frame_shift` (independent of the STFT's own frame count, matching the source exactly), RANDOM crop position for clips longer than 750 frames (preserved as published, including at eval time -- not replaced with a deterministic centre crop, since that would deviate from the published evaluation protocol; run-to-run reproducibility comes from the pipeline's existing global seeding, not a new per-module RNG), tile-repeat for shorter clips. Verified locally (CPU, random init, no checkpoint) before touching the server: both the long-clip (random-crop) and short-clip (tile) branches run end-to-end with zero shape errors -> (B,2) logits; 875,080 total / 869,536 trainable parameters, frozen difference reconciling exactly to 5,544 (same LFCC filterbank+DCT+legacy-buffer count as the lstm-sum backend, an independent cross-check the wiring is right). STILL BLOCKED: the SelfWeightedPooling backend (true architecture of `lfcc-lcnn-attention-p2s`, weights in `trained_network_fix.pt`) needs `SelfWeightedPooling`'s class source from `sandbox/block_nn.py`, not yet fetched -- refusing to guess its internals from the `mm_weights(1,96)` shape alone. `trained_network_finetune_att.pt`'s true architecture is ALSO unconfirmed given the swap just found -- cannot assume it follows the "finetune shares its base's arch" pattern that held for lstm without checking.
- **2026-08-25 (cont. 7)** -- **CORRECTION to the "checkpoint filename swap" claim logged two entries ago: there was no swap.** That conclusion was reached by attributing shape-dump blocks to checkpoint filenames based on their POSITION in a batched, multi-command terminal paste -- unreliable, since the same paste-mangling causing `-bash: syntax error` noise all session also scrambles output ordering. Resolved with an unambiguous single-process fingerprint script that loads all six checkpoints and prints each result labeled by its OWN filename (no positional inference possible): `trained_network.pt`->LSTM-SUM(61), `trained_network_lstm.pt`->LSTM-SUM(61), `trained_network_att.pt`->SELFWEIGHTEDPOOLING(46), `trained_network_fix.pt`->FIXED-750-CROP(47), `trained_network_finetune_lstm.pt`->LSTM-SUM(61), `trained_network_finetune_att.pt`->SELFWEIGHTEDPOOLING(46). Jaime's filenames are exactly what they look like -- "att"=SelfWeightedPooling (a legitimate self-attention pooling mechanism), "fix"=fixed-750-frame truncation, "lstm"=BLSTM-sum -- no relabeling needed anywhere. `lcnn_fixedcrop/settings.py` CHECKPOINT_PATH corrected from `trained_network_att.pt` (wrong, caused the `strict=True` load to correctly reject it with a clean missing/unexpected-key error -- the safety mechanism worked exactly as designed) to `trained_network_fix.pt` (verified correct architecture match). Docstrings in both files rewritten to state the true, verified mapping instead of the retracted swap claim. Checkpoint inventory now fully resolved: SelfWeightedPooling has TWO usable checkpoints (base `att.pt` + finetuned `finetune_att.pt`); fixed-750-crop has only the base (`fix.pt`, no finetuned counterpart exists on disk); LSTM-sum has base + finetuned + the unsuffixed duplicate. Lesson: verify architecture-to-checkpoint mapping via one process printing self-labeled results, never via positional inference across a multi-command paste.
