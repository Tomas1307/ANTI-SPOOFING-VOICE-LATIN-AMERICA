"""Standalone operational scripts for the anti-spoofing project.

Marks ``scripts`` as a package so its modules can be run with
``python3 -m scripts.<name>`` from the repository root (which places the root
on sys.path so ``import app...`` resolves without a PYTHONPATH prefix).
"""
